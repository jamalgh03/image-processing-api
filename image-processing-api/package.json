{
  "name": "image-processing-api",
  "version": "1.0.0",
  "scripts": {
    "start": "node -e \"require('./dist/src/index').listen(3000, () => console.log('Server running on port 3000'))\"",
    "build": "tsc",
    "test": "npm run build && jasmine",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sharp": "^0.32.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/jasmine": "^4.3.0",
    "@types/node": "^18.0.0",
    "@types/supertest": "^6.0.0",
    "eslint": "^8.0.0",
    "jasmine": "^4.5.0",
    "prettier": "^2.8.0",
    "supertest": "^6.3.0",
    "typescript": "^5.0.0"
  }
}