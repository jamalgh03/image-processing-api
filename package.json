{
  "name": "image-processing-api",
  "version": "1.0.0",
  "scripts": {
    "start": "cd image-processing-api && node dist/src/index.js",
    "build": "cd image-processing-api && tsc",
    "test": "cd image-processing-api && npm run build && cd .. && jasmine",
    "lint": "eslint image-processing-api/src/**/*.ts",
    "format": "prettier --write image-processing-api/src/**/*.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/jasmine": "^4.3.0",
    "@types/node": "^18.0.0",
    "@types/supertest": "^7.2.0",
    "eslint": "^9.0.0",
    "jasmine": "^4.5.0",
    "prettier": "^3.0.0",
    "supertest": "^7.1.3",
    "typescript": "^5.0.0"
  }
}